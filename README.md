# Smart Trash - Интерактивный помощник по раздельному сбору отходов

Современное веб-приложение для определения типа отходов и получения инструкций по правильной утилизации с элементами геймификации.

## 🚀 Технологический стек

- **React 18** + **TypeScript** - современный UI фреймворк с типобезопасностью
- **Vite** - быстрая сборка и разработка
- **Tailwind CSS** - утилитарный CSS фреймворк для быстрой стилизации
- **Apollo Client** - клиент для работы с GraphQL API
- **Framer Motion** - библиотека для плавных анимаций
- **React Router** - маршрутизация
- **Zustand** - легковесное управление состоянием
- **Lucide React** - красивые иконки

## ✨ Основные возможности

### Для пользователей (планшетный интерфейс)

1. **Определение типа отхода**
   - 📸 Фотографирование отхода с автоматической классификацией через AI
   - 📋 Ручной выбор типа из списка
   - 📱 Сканирование штрихкода (в разработке)

2. **Подсказки и инструкции**
   - 🎯 Визуальная индикация нужного контейнера
   - 📝 Пошаговые инструкции по подготовке отхода
   - 💡 Объяснение от AI о правильной утилизации

3. **Геймификация**
   - 🏆 Таблица лидеров
   - ⭐ Система очков за каждую сортировку
   - 📊 Статистика использования

### Для администраторов

- 📊 Аналитика по использованию контейнеров
- 👥 Управление сотрудниками
- ⚙️ Настройка областей сбора и контейнеров

## 📦 Установка и запуск

### Требования

- Node.js 18+ 
- npm или yarn
- Запущенный бэкенд (NestJS GraphQL сервер)

### Быстрый старт

1. **Перейдите в папку Frontend:**

```bash
cd Frontend
```

2. **Установите зависимости:**

```bash
npm install
```

3. **Настройте переменные окружения:**

Скопируйте `.env.example` в `.env` и настройте:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# GraphQL API endpoint
VITE_GRAPHQL_URL=http://localhost:4000/graphql

# Backend API base URL (для загрузки файлов)
VITE_API_URL=http://localhost:4000

# ID вашей компании (получите из бэкенда)
VITE_DEFAULT_COMPANY_ID=your-company-id-here

# ID области сбора (опционально)
VITE_DEFAULT_COLLECTION_AREA_ID=
```

4. **Запустите dev-сервер:**

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

5. **Сборка для production:**

```bash
npm run build
```

Собранные файлы будут в папке `dist/`. Для production используйте любой статический хостинг (nginx, Vercel, Netlify и т.д.)

## 🎨 Особенности дизайна

- **Адаптивный дизайн** - оптимизирован для планшетов в горизонтальной ориентации
- **Современный UI** - стеклянный эффект (glassmorphism), градиенты, плавные анимации
- **Интуитивная навигация** - крупные кнопки, понятные иконки
- **Цветовая кодировка** - каждый тип контейнера имеет свой цвет и иконку

## 📱 Использование

### Основной сценарий

1. Откройте приложение на планшете
2. Выберите способ определения отхода:
   - **Сфотографировать** - сделайте фото, система определит тип автоматически
   - **Выбрать вручную** - выберите тип из списка
3. Получите рекомендацию по контейнеру и инструкции
4. Выбросьте отход в правильный контейнер
5. Получите очки и посмотрите свой рейтинг

### Типы контейнеров

- 🗑️ **Смешанные отходы** - для неперерабатываемых материалов
- ♻️ **Пластик** - бутылки, контейнеры, упаковка
- 📄 **Бумага** - картон, газеты, документы
- 🍾 **Стекло** - бутылки, банки
- 🥫 **Металл** - банки, фольга
- 🍌 **Органика** - пищевые отходы
- 🔌 **Электроника** - устройства, батарейки
- ⚠️ **Опасные отходы** - химикаты, лампы

## 🔧 Настройка для вашей компании

### Получение companyId

1. Запустите бэкенд сервер
2. Откройте GraphQL Playground (обычно `http://localhost:4000/graphql`)
3. Выполните запрос для создания или получения компании
4. Скопируйте `id` компании в переменную `VITE_DEFAULT_COMPANY_ID` в `.env`

### Настройка области сбора

1. Создайте область сбора через админ-панель или GraphQL
2. Укажите `id` области в `VITE_DEFAULT_COLLECTION_AREA_ID` (опционально)

### Альтернативный способ

Вы можете настроить эти значения программно через store в `src/store/useWasteStore.ts` или через UI приложения (если такая функция будет добавлена).

## 🐛 Решение проблем

### Ошибка подключения к GraphQL

- Проверьте, что бэкенд запущен на порту 4000
- Убедитесь, что `VITE_GRAPHQL_URL` указан правильно
- Проверьте CORS настройки на бэкенде

### Фото не загружается

- Проверьте, что модуль загрузки файлов на бэкенде работает
- Убедитесь, что endpoint `/images/upload` доступен (должен быть `POST /images/upload`)
- Проверьте размер файла (максимум 10MB)
- Убедитесь, что формат файла поддерживается (jpg, jpeg, png, gif)
- Проверьте CORS настройки на бэкенде

### Ошибки TypeScript

- Убедитесь, что все зависимости установлены: `npm install`
- Проверьте версию Node.js (должна быть 18+)
- Попробуйте удалить `node_modules` и `package-lock.json`, затем `npm install` заново

## 📄 Лицензия

Этот проект создан в рамках хакатона по раздельному сбору отходов.

## 👥 Команда разработки

Разработано с ❤️ для экологичного будущего

---

**Примечание:** Для полноценной работы приложения необходим запущенный бэкенд сервер. Убедитесь, что все зависимости установлены и сервисы запущены перед использованием.

