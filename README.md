# Smart Trash Frontend

![React](https://img.shields.io/badge/React-18.2.0-61DAFB?logo=react)
![TypeScript](https://img.shields.io/badge/TypeScript-5.3.3-3178C6?logo=typescript)
![Vite](https://img.shields.io/badge/Vite-5.0.11-646CFF?logo=vite)
![Tailwind CSS](https://img.shields.io/badge/Tailwind-3.4.1-38B2AC?logo=tailwind-css)

Frontend приложение для системы управления сортировкой отходов, построенное на React 18 с TypeScript и Vite.

## 📋 Содержание

- [Технологии](#технологии)
- [Требования](#требования)
- [Быстрый старт](#быстрый-старт)
- [Установка и настройка](#установка-и-настройка)
- [Структура проекта](#структура-проекта)
- [Маршруты](#маршруты)
- [Разработка](#разработка)
- [Production сборка](#production-сборка)
- [Troubleshooting](#troubleshooting)

## 🛠 Технологии

### Основной стек
- **[React](https://react.dev/)** 18.2.0 - UI библиотека
- **[TypeScript](https://www.typescriptlang.org/)** 5.3.3 - Типизация
- **[Vite](https://vitejs.dev/)** 5.0.11 - Сборщик и dev-сервер
- **[Tailwind CSS](https://tailwindcss.com/)** 3.4.1 - Utility-first CSS фреймворк

### Дополнительные библиотеки
- **[Apollo Client](https://www.apollographql.com/docs/react/)** - GraphQL клиент
- **[React Router](https://reactrouter.com/)** - Маршрутизация
- **[Zustand](https://zustand-demo.pmnd.rs/)** - Управление состоянием
- **[React Hook Form](https://react-hook-form.com/)** - Работа с формами
- **[Framer Motion](https://www.framer.com/motion/)** - Анимации
- **[React Hot Toast](https://react-hot-toast.com/)** - Уведомления
- **[Lucide React](https://lucide.dev/)** - Иконки
- **[HTML5 QR Code](https://github.com/mebjas/html5-qrcode)** - Сканирование QR-кодов

## 📦 Требования

- **Node.js** 18+ ([скачать](https://nodejs.org/))
- **npm** или **yarn**
- **Запущенный backend** (см. [Backend README](../Smart_trash_back/README.md))

## 🚀 Быстрый старт

```bash
# 1. Установить зависимости
npm install

# 2. (Опционально) Создать .env.local файл
# По умолчанию используются значения:
# VITE_API_URL=http://localhost:5000
# VITE_GRAPHQL_URL=http://localhost:5000/graphql

# 3. Запустить frontend
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

## ⚙️ Установка и настройка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта (опционально):

```env
# URL backend API
VITE_API_URL=http://localhost:5000

# URL GraphQL endpoint
VITE_GRAPHQL_URL=http://localhost:5000/graphql

# (Опционально) ID компании по умолчанию
VITE_DEFAULT_COMPANY_ID=

# (Опционально) ID зоны сбора по умолчанию
VITE_DEFAULT_COLLECTION_AREA_ID=
```

**Примечание:** Если файл `.env.local` не создан, используются значения по умолчанию.

### 3. Запуск frontend

#### Режим разработки:

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

#### Другие команды:

```bash
# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview

# Линтинг
npm run lint

# Генерация GraphQL типов
npm run codegen
```

## 📂 Структура проекта

```
src/
├── components/           # React компоненты
│   ├── ui/              # Переиспользуемые UI компоненты
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Input.tsx
│   │   └── Modal.tsx
│   ├── ProtectedRoute.tsx    # Защита маршрутов
│   ├── PhotoUploader.tsx      # Загрузка фотографий
│   ├── WasteMethodSelector.tsx # Выбор метода сортировки
│   ├── ManualSelector.tsx     # Ручной выбор типа отхода
│   ├── BinLayout.tsx          # Макет контейнеров
│   └── AchievementsList.tsx   # Список достижений
│
├── pages/               # Страницы приложения
│   ├── LandingPage.tsx        # Главная страница
│   ├── LoginPage.tsx          # Вход
│   ├── RegisterPage.tsx      # Регистрация
│   ├── ConfirmEmailPage.tsx   # Подтверждение email
│   ├── AdminDashboard.tsx     # Панель администратора
│   ├── EmployeeDashboard.tsx  # Панель сотрудника
│   ├── HomePage.tsx           # Домашняя страница
│   ├── AnalyticsPage.tsx      # Аналитика
│   ├── LeaderboardPage.tsx    # Рейтинг
│   ├── AchievementsPage.tsx   # Достижения
│   ├── WasteHistoryPage.tsx   # История сортировок
│   ├── ProfilePage.tsx        # Профиль
│   └── ...
│
├── lib/                 # Библиотеки и утилиты
│   ├── apollo.ts       # Apollo Client конфигурация
│   ├── graphql/        # GraphQL запросы и мутации
│   │   ├── queries.ts
│   │   └── mutations.ts
│   └── utils.ts        # Вспомогательные функции
│
├── store/              # Zustand хранилища
│   └── useWasteStore.ts # Состояние сортировки отходов
│
├── types/              # TypeScript типы
│   └── index.ts
│
├── App.tsx             # Главный компонент с роутингом
├── main.tsx            # Точка входа
└── index.css           # Глобальные стили
```

## 🗺 Маршруты

### Публичные страницы

| Маршрут | Описание |
|---------|----------|
| `/` | Главная страница (Landing) |
| `/login` | Вход в систему |
| `/register` | Регистрация администратора компании |
| `/employee/register` | Регистрация сотрудника |
| `/confirm-email` | Подтверждение email |
| `/about` | О компании |
| `/how-it-works` | Как это работает |
| `/for-companies` | Для компаний |
| `/contacts` | Контакты |

### Защищенные страницы (требуют авторизации)

#### Для администраторов (`ADMIN_COMPANY`)

| Маршрут | Описание |
|---------|----------|
| `/admin/dashboard` | Панель администратора |
| `/admin/employees` | Управление сотрудниками |
| `/admin/collection-areas` | Управление зонами сбора |
| `/admin/bins` | Управление контейнерами |
| `/admin/analytics` | Аналитика компании |
| `/admin/company-settings` | Настройки компании |
| `/admin/achievements` | Управление достижениями |

#### Для сотрудников (`EMPLOYEE`)

| Маршрут | Описание |
|---------|----------|
| `/employee/dashboard` | Панель сотрудника |
| `/home` | Главная страница сортировки |
| `/employee/leaderboard` | Рейтинг сотрудников |
| `/employee/achievements` | Достижения сотрудника |
| `/employee/history` | История сортировок |
| `/profile` | Профиль пользователя |

#### Общие страницы

| Маршрут | Описание |
|---------|----------|
| `/result` | Результат классификации отхода |
| `/barcode-scanner` | Сканер штрих-кодов |
| `/public-mode` | Публичный режим (без авторизации) |

## 💻 Разработка

### Функциональность

#### Аутентификация
- ✅ Регистрация администратора компании
- ✅ Регистрация сотрудника по приглашению
- ✅ Вход в систему (JWT токены)
- ✅ Подтверждение email
- ✅ Защищенные маршруты (`ProtectedRoute`)
- ✅ Автоматическое перенаправление при отсутствии авторизации

#### Управление отходами
- ✅ Фото классификация через AI (GigaChat)
- ✅ Ручной выбор типа отхода
- ✅ Сканирование штрих-кода
- ✅ История сортировок
- ✅ Статистика по типам отходов
- ✅ Публичный режим (без авторизации)

#### Геймификация
- ✅ Система достижений
- ✅ Рейтинг сотрудников
- ✅ Прогресс по целям
- ✅ Награды и бейджи

#### Аналитика
- ✅ Статистика по компании
- ✅ Статистика по сотрудникам
- ✅ Графики и диаграммы
- ✅ Фильтрация по датам

### API интеграция

Frontend использует GraphQL API backend через Apollo Client.

#### Основные запросы (Queries)

```typescript
GET_ME              // Текущий пользователь
GET_COMPANIES       // Список компаний
GET_COMPANY         // Информация о компании
GET_COLLECTION_AREAS // Зоны сбора
GET_EMPLOYEES       // Список сотрудников
GET_WASTE_PHOTOS    // История сортировок
GET_ANALYTICS       // Аналитика компании
GET_ACHIEVEMENTS    // Достижения
```

#### Основные мутации (Mutations)

```typescript
REGISTER_ADMIN      // Регистрация администратора
REGISTER_EMPLOYEE   // Регистрация сотрудника
LOGIN              // Вход в систему
CONFIRM_EMAIL      // Подтверждение email
CREATE_WASTE_PHOTO // Создание фотографии отхода
UPDATE_COMPANY     // Обновление компании
CREATE_ACHIEVEMENT // Создание достижения
```

### Стилизация

Проект использует **Tailwind CSS** с кастомными компонентами:

- **GreenGradientBackground** - Градиентный фон
- **Button** - Кнопки с вариантами (primary, secondary, outline)
- **Card** - Карточки для контента
- **Input** - Поля ввода
- **Modal** - Модальные окна

Адаптивный дизайн для:
- 📱 Мобильных устройств
- 📱 Планшетов
- 💻 Десктопов

### State Management

Используется **Zustand** для управления состоянием:

#### `useWasteStore`
Управление состоянием сортировки отходов:
- Выбранный метод сортировки
- Загруженное изображение
- Результат классификации
- История сортировок

### GraphQL Code Generation

Для генерации TypeScript типов из GraphQL схемы:

```bash
npm run codegen
```

Это создаст типизированные хуки для запросов и мутаций.

## 🚢 Production сборка

### Сборка проекта

```bash
npm run build
```

Результат будет в папке `dist/`, готовый для развертывания на любом статическом хостинге.

### Предпросмотр production сборки

```bash
npm run preview
```

### Развертывание

#### Статический хостинг

Собранные файлы из `dist/` можно развернуть на:
- **Vercel**
- **Netlify**
- **GitHub Pages**
- **AWS S3 + CloudFront**
- Любой другой статический хостинг

#### Настройка для production

1. Обновите переменные окружения:
   ```env
   VITE_API_URL=https://api.yourdomain.com
   VITE_GRAPHQL_URL=https://api.yourdomain.com/graphql
   ```

2. Пересоберите проект:
   ```bash
   npm run build
   ```

3. Загрузите содержимое `dist/` на хостинг

## 👤 Тестовый администратор

Для быстрого старта в системе уже создан тестовый администратор:

- **Email:** `admin@smarttrash.ru`
- **Пароль:** `admin123`
- **Роль:** `ADMIN_COMPANY`
- **Email подтвержден:** ✅ Да

Вы можете использовать эти учетные данные для входа в систему сразу после запуска проекта.

Для создания нового администратора используйте команду в backend:
```bash
cd Smart_trash_back
npm run create:admin
```

## 🐛 Troubleshooting

### Ошибка подключения к backend

1. Убедитесь, что backend запущен на `http://localhost:5000`:
   ```bash
   # Проверка порта
   netstat -ano | findstr :5000  # Windows
   lsof -i :5000                 # Linux/Mac
   ```

2. Проверьте переменные окружения в `.env.local`:
   ```env
   VITE_API_URL=http://localhost:5000
   VITE_GRAPHQL_URL=http://localhost:5000/graphql
   ```

3. Проверьте CORS настройки на backend (должен быть разрешен `http://localhost:3000`)

4. Откройте консоль браузера (F12) и проверьте ошибки сети

### Ошибки компиляции TypeScript

```bash
# Очистить кэш и переустановить зависимости
rm -rf node_modules dist .vite
npm install

# Перезапустить dev-сервер
npm run dev
```

### Проблемы с hot-reload

1. Перезапустите dev-сервер:
   ```bash
   # Остановить (Ctrl+C) и запустить снова
   npm run dev
   ```

2. Очистите кэш Vite:
   ```bash
   rm -rf .vite
   npm run dev
   ```

### Ошибки Apollo Client

1. Проверьте, что GraphQL endpoint доступен:
   ```bash
   curl http://localhost:5000/graphql
   ```

2. Проверьте конфигурацию Apollo Client в `src/lib/apollo.ts`

3. Проверьте токен авторизации в localStorage:
   ```javascript
   localStorage.getItem('auth_token')
   ```

### Проблемы с изображениями

1. Убедитесь, что backend доступен
2. Проверьте настройки CORS
3. Проверьте размер загружаемых файлов (максимальный размер настраивается на backend)

### Проблемы с Tailwind CSS

Если стили не применяются:

1. Проверьте `tailwind.config.js`
2. Убедитесь, что `index.css` импортирован в `main.tsx`
3. Перезапустите dev-сервер

## 📝 Полезные команды

```bash
# Разработка
npm run dev              # Запуск dev-сервера
npm run build            # Сборка для production
npm run preview          # Предпросмотр production сборки

# Качество кода
npm run lint             # Линтинг
npm run lint -- --fix    # Автоисправление ошибок линтера

# GraphQL
npm run codegen          # Генерация типов из GraphQL схемы
```

## 🔗 Полезные ссылки

- [React Documentation](https://react.dev/)
- [TypeScript Documentation](https://www.typescriptlang.org/docs/)
- [Vite Documentation](https://vitejs.dev/)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [Apollo Client Documentation](https://www.apollographql.com/docs/react/)
- [React Router Documentation](https://reactrouter.com/)
- [Zustand Documentation](https://zustand-demo.pmnd.rs/)

## 📝 Лицензия

MIT License

---

**Примечание:** Убедитесь, что backend запущен перед запуском frontend!
